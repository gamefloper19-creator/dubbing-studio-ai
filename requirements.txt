# Dubbing Studio - Dependencies
# ================================

# Core
gradio>=4.0.0

# Speech Recognition
openai-whisper>=20231117

# Translation
google-generativeai>=0.5.0

# TTS Engines (edge-tts as universal fallback)
edge-tts>=6.1.0

# Audio Processing
# FFmpeg must be installed separately (system package)

# Hardware Detection
psutil>=5.9.0

# Desktop GUI
PySide6>=6.5.0

# YouTube Download
# yt-dlp must be installed separately: pip install yt-dlp

# Packaging
# PyInstaller for building standalone executable
# pip install pyinstaller

# Optional: Native TTS engines (install separately if needed)
# pip install chatterbox-tts
# pip install TTS
# pip install transformers torch

# Optional: Voice cloning
# pip install resemblyzer
# pip install TTS  (for XTTS-v2)

# Optional: Speaker diarization
# pip install pyannote.audio

# Optional: YouTube upload
# pip install google-api-python-client google-auth-oauthlib
